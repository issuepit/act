on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: normal-step
        id: normal-step
        run: echo "normal step ran"
        shell: bash
      - name: skipped-step
        id: skipped-step
        run: exit 1
        shell: bash
